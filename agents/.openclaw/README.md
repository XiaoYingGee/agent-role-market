# OpenClaw Adapter

This adapter provides a generic OpenClaw-style skill package.

Suggested use:

```text
Copy agents/.openclaw/skills/agent-role-market/SKILL.md into your OpenClaw skills directory.
```

This adapter does not include workspace paths, agent names, bot IDs, channel IDs, or chat-platform-specific rules.
